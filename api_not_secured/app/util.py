#call the dynanmodb to get account name
#call the dynamodb to get relevant schema names

# get sp api schema nam
def get_sp_api_schema_name(sub: str):
    #logic to get the schema name
    return "sp_api_schema_name"

# get ads api schema name

# get account name